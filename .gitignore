# Node / web
node_modules/
dist/
.vite/

# .NET
bin/
obj/
*.user

# EF Core / local secrets
.env
appsettings.*.local.json

# Uploaded chore-completion / event-import photos (local dev / bind-mounted in Docker)
src/ChaosCoordinator.Api/uploads/*
!src/ChaosCoordinator.Api/uploads/.gitkeep

# ASP.NET Data Protection keyring (local dev) — see Program.cs's DATA_PROTECTION_KEYS_PATH comment
src/ChaosCoordinator.Api/keys/

# OS / editor
.DS_Store
Thumbs.db
.vs/
.vscode/

# Design handoff working copy (source zip stays, extracted scratch copy doesn't need to be tracked)
/extracted/
